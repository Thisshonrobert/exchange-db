{
  "name": "db",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
  "dev": "bun run src/index.ts",
  "start": "bun run src/index.ts",
  "seed:db": "bun run src/seed-db.ts",
  "refresh:views": "bun run src/cron.ts",
  "build": "bun build ./src/index.ts --outdir dist"
},

  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/pg": "^8.15.6",
    "pg": "^8.16.3",
    "redis": "^5.9.0"
  }
}
